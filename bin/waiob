#!/usr/bin/env bash

export SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )/.." &> /dev/null && pwd )

export SRC_DIR="${SRC_DIR:-"${SCRIPT_DIR}/src"}"
export LIB_DIR="${LIB_DIR:-"${SRC_DIR}/lib"}"

source "${SCRIPT_DIR}/src/main.sh"